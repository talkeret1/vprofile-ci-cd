services:
  jenkins:
    container_name: jenkins
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

    build: .
    ports:
      - "8080:8080"
      - "5000:5000"

volumes:
  jenkins_home:
